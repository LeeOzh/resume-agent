# ResumeAgent UI 现代化改造方案

> 2026-08-12 · 目标：在不改动业务逻辑（浏览器/任务/数据库/下载流程）的前提下，把 GUI 从“工具风”升级为现代精致的桌面应用观感。

## 1. 结论

**可以，且推荐留在 PyQt6 上做深度美化。**

当前技术栈：PyQt6 6.11 + Qt Widgets + 手写 QSS（约 7.7KB）。
PyQt6/Qt6 是完全成熟的现代 UI 框架，圆角、阴影、动画、SVG 图标、明暗主题、
自定义无边框窗口等能力都具备。观感偏旧的真正原因是**样式与组件设计停留在“扁平工具风”**：

- 系统默认窗口边框 + 标题栏，无圆角、无阴影
- 控件间距/圆角/颜色层级缺乏统一设计规范
- 无图标、无动画、无状态徽标、无空状态
- 表格、分组框、日志等区域视觉平淡
- 没有明暗主题切换

以上全部可在 PyQt6 内解决，改造只动 `gui/` 目录，不影响
`browser_worker / download_worker / task / db` 任何现有逻辑。

---

## 2. 可选方案对比

| 方案 | 视觉效果 | 工作量 | 风险 | 结论 |
|------|---------|--------|------|------|
| **A. PyQt6 深度美化** | 高（接近现代原生应用） | 中（约 2~4 天） | 低 | **推荐** |
| B. Qt Quick / QML | 高（动效更强） | 高（GUI 层重写） | 中 | 不推荐，代价大于收益 |
| C. Web UI（pywebview + HTML/CSS） | 最高（可套 Ant Design 等） | 中高（GUI 层重写 + IPC） | 中 | 备选：若后续要做复杂可视化再考虑 |
| D. 换框架（Tauri / Electron / WPF） | 高 | 很高（全栈重写） | 高 | 不推荐，Python 后端无法保留 |

---

## 3. 推荐方案 A：PyQt6 深度美化（分阶段）

### 阶段 1：视觉基础（设计系统）

**交付：重构 `default.qss` + 新增主题文件**

- 设计令牌：主色 / 成功 / 危险 / 警告 / 中性色阶，8px 间距栅格，统一圆角（4/8/12px）
- 字体：Microsoft YaHei UI，字号层级（标题/正文/辅助/代码）
- 控件状态全覆盖：normal / hover / pressed / checked / disabled / focus
- 明暗双主题：默认亮色，设置里可切换暗色
- 参考起点：qt-material / QDarkStyleSheet，取其设计语言再定制品牌色

### 阶段 2：无边框窗口 + 自定义标题栏

**交付：`gui/windows/title_bar.py` + 窗口阴影**

- 去掉系统标题栏（`FramelessWindowHint`），自绘标题栏：
  - 窗口拖拽、双击最大化/还原
  - 最小化 / 最大化 / 关闭按钮（SVG 图标 + hover 反馈）
  - 窗口圆角 + 投影（QGraphicsDropShadowEffect）
- 标题栏左侧放应用名，右侧放“设置 / 关于”入口
- 这一步完成观感提升最明显

### 阶段 3：组件精细化

**交付：`gui/widgets/` 新增可复用组件**

- 候选人表格：
  - 表头吸顶、行 hover 高亮、斑马纹
  - 状态徽标（已下载绿 / 失败红 / AI淘汰橙，圆点+文字）
  - 空状态插画提示（“未获取到候选人”）
  - 数字列右对齐、姓名加粗
- 卡片化面板：QFrame 卡片替代 QGroupBox（阴影、圆角、留白、分组标题）
- 按钮体系：主按钮（品牌色）/ 次按钮（描边）/ 危险按钮，配图标与 hover 过渡
- 图标：内嵌 SVG 图标集（刷新/下载/暂停/继续/设置/日志/状态）
- 日志区：时间线样式、按级别着色（info/warning/error）、自动滚动
- 进度：任务进度条分段动画；候选人计数徽标

### 阶段 4：布局与交互

**交付：主窗口结构微调**

- 右侧控制面板卡片化：职位选择 / 学校筛选 / 下载控制分卡
- 状态栏升级为“状态条”：浏览器状态 + 任务状态 + 候选人计数做成带色点的状态组件
- 交互动效：面板展开、按钮过渡、弹窗淡入（QPropertyAnimation，控制在几十 ms 内不拖沓）
- 快捷键：F5 刷新、Ctrl+Enter 开始下载、Esc 中断
- 高 DPI 适配检查（缩放 100% / 125% / 150%）

### 阶段 5：验收与打包

**交付：可测试 exe**

- 全流程冒烟：启动 → 连 Chrome → 刷新 → 筛选 → 下载 → 暂停/恢复 → 失败记录显示
- 视觉检查清单：明暗主题、窗口缩放、中文渲染、无控件挤压错位
- 重新打包 exe

---

## 4. 风险与约束

- 只改 `gui/` 层，`browser_worker / download_worker / task / db / main_gui` 不动，回归风险低
- Qt Widgets 的动效上限低于 Web/QML，但本工具（表格+表单+日志）完全够用
- 打包体积几乎不变；不引入新依赖（或仅引入一个 QSS 主题库）
- 若未来需要复杂可视化/富交互，可再评估方案 C（Web UI），届时后端 worker 可复用

---

## 5. 建议执行方式

1. **先出视觉原型**：完成阶段 1+2（设计令牌 + 无边框窗口 + 主界面重排），生成可运行的预览版，你确认风格后继续阶段 3~5
2. 每个阶段结束提交一次，随时可回归
3. 全部完成后更新 README / PROGRESS，重新打包
