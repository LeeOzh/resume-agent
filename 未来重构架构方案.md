# ResumeAgent 未来重构架构方案（存档）

> 2026-08-12 · 当前决定：**暂不实施**，先做 PyQt6 深度美化。
> 本文档存档，供后期评估重构时使用。

## 1. 目标架构

```text
ResumeAgent
│
├── frontend/
│   ├── React
│   ├── TypeScript
│   ├── Tailwind
│   └── shadcn/ui
│
├── backend/
│   ├── Python
│   ├── FastAPI
│   ├── Playwright
│   ├── BrowserManager
│   ├── TaskManager
│   ├── LLM
│   └── SQLite
│
└── desktop/
    └── Tauri
```

## 2. 选择理由

- UI 质量天花板最高：shadcn/ui + Tailwind，暗色模式/表格/徽标/弹窗开箱即用
- 实时日志/进度：FastAPI + WebSocket 推流，优于当前 multiprocessing Queue + QTimer 轮询
- 前后端分离后，改 UI 不碰业务逻辑
- Tauri 壳体积小（10~20MB），无 Electron 包袱

## 3. 主要风险

1. **Tauri + Python 打包（最大风险）**：Python 后端需 PyInstaller 打成 sidecar，
   由 Tauri 管理生命周期（启动/停止、端口冲突、崩溃重启），
   Playwright 浏览器（约 150MB+）需捆绑并配置路径
2. **本地 HTTP 服务安全**：绑 127.0.0.1 + 随机端口 + token 鉴权 + CORS 限制
3. **工程量是重写级**：GUI 胶水层全部重写；BrowserManager/TaskManager/download_worker
   核心逻辑可复用，不重写
4. **开发环境变重**：Node + pnpm + Rust + Python 两套环境

## 4. 建议落地顺序（降风险）

### 第一步：本地 Web 版（不做 Tauri）

- 现有 Python 核心包成 FastAPI 本地服务（127.0.0.1 + 随机端口 + token）
- WebSocket 推送日志/任务进度
- React + Vite + TS + Tailwind + shadcn/ui 最小可用页面
- 浏览器打开使用 —— 先拿到 90% 视觉收益，零打包风险

### 第二步：功能对齐

- 候选人表格（状态徽标/历史记录/学校筛选）
- 下载控制（开始/暂停/继续/中断）、任务历史、AI 配置、设置
- 与当前 PyQt6 版功能对齐

### 第三步：套 Tauri 壳

- Tauri v2 + PyInstaller sidecar 方案
- 双击启动的桌面应用，生命周期与崩溃恢复完善

## 5. API 设计草案

```text
GET  /api/jobs                    岗位列表（刷新/同步）
GET  /api/candidates              候选人列表（滚动获取 + 历史状态）
POST /api/tasks                   创建任务（含 AI 配置快照）
GET  /api/tasks                  任务列表/未完成任务
POST /api/tasks/{id}/start       开始下载
POST /api/tasks/{id}/pause       暂停
POST /api/tasks/{id}/resume      恢复
POST /api/tasks/{id}/cancel      放弃
GET  /api/tasks/{id}/stats       任务统计
WS   /api/ws/logs                日志/进度实时推流
```

## 6. 结论

- 架构本身是行业标准组合，长期可维护性好
- 对当前项目属“重写”，工作量约 2~4 周（vs PyQt6 美化 2~4 天）
- 若后续需要复杂可视化/富交互/多端，再启用本方案
